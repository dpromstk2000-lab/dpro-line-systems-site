/* DPRO PRODUCT SITE V3.0 — COMMON DESIGN SYSTEM */
:root {
  --dpro-deep-green: #0f3d2e;
  --dpro-green: #16845b;
  --dpro-green-strong: #0f6e4a;
  --dpro-white: #ffffff;
  --dpro-soft-green: #edf7f2;
  --dpro-warm-gray-50: #faf9f7;
  --dpro-warm-gray-100: #f1efeb;
  --dpro-warm-gray-300: #d8d3cc;
  --dpro-warm-gray-600: #6b665f;
  --dpro-warm-gray-900: #26231f;
  --dpro-line-green: #06c755;
  --dpro-danger: #b42318;

  --dpro-font-xs: clamp(.75rem, .72rem + .1vw, .8rem);
  --dpro-font-sm: clamp(.875rem, .84rem + .12vw, .95rem);
  --dpro-font-md: clamp(1rem, .96rem + .16vw, 1.08rem);
  --dpro-font-lg: clamp(1.15rem, 1.05rem + .4vw, 1.35rem);
  --dpro-font-xl: clamp(1.5rem, 1.25rem + .9vw, 2rem);
  --dpro-font-2xl: clamp(2rem, 1.55rem + 1.8vw, 3.15rem);

  --dpro-space-1: .25rem;
  --dpro-space-2: .5rem;
  --dpro-space-3: .75rem;
  --dpro-space-4: 1rem;
  --dpro-space-5: 1.5rem;
  --dpro-space-6: 2rem;
  --dpro-space-7: 3rem;
  --dpro-space-8: 4.5rem;

  --dpro-radius-sm: .55rem;
  --dpro-radius-md: .85rem;
  --dpro-radius-lg: 1.25rem;
  --dpro-radius-pill: 999px;

  --dpro-shadow-sm: 0 2px 10px rgba(15,61,46,.08);
  --dpro-shadow-md: 0 12px 32px rgba(15,61,46,.12);
  --dpro-shadow-lg: 0 22px 56px rgba(15,61,46,.16);
  --dpro-focus: 0 0 0 3px rgba(22,132,91,.28);
  --dpro-content: 1120px;
}

*, *::before, *::after { box-sizing: border-box; }
html { -webkit-text-size-adjust: 100%; overflow-x: hidden; }
body { margin: 0; min-width: 0; overflow-x: hidden; color: var(--dpro-warm-gray-900); background: var(--dpro-white); }

/* Japanese typography: prevent orphaned characters and awkward phrase splits. */
:lang(ja) {
  word-break: normal;
  line-break: strict;
  overflow-wrap: normal;
}
@supports (word-break: auto-phrase) {
  :lang(ja) { word-break: auto-phrase; }
}

:where(
  .core-hero h1,
  .core-heading h2,
  .core-cta h2,
  .core-browser__body h2,
  .core-card h3,
  .ps-hero h1,
  .ps-section-heading h2,
  .ps-preview-card h2,
  .ps-card-copy h3,
  .ps-function h3,
  .ps-flow-step h3,
  .ps-live-copy h3,
  .ps-final h2
) {
  text-wrap: balance;
}

/* Desktop-authored <br> tags should not force unnatural breaks on narrower layouts. */
@media (max-width: 900px) {
  :where(
    .core-hero h1,
    .core-heading h2,
    .core-cta h2,
    .ps-hero h1,
    .ps-section-heading h2,
    .ps-final h2
  ) br {
    display: none;
  }
}

img, video, svg, iframe { max-width: 100%; }
img, video { height: auto; }
a, button, input, select, textarea { font: inherit; }
button, [role="button"], a.dpro-button { min-height: 44px; }
:focus-visible { outline: 2px solid var(--dpro-green); outline-offset: 3px; box-shadow: var(--dpro-focus); }

.dpro-container { width: min(calc(100% - 2rem), var(--dpro-content)); margin-inline: auto; }
.dpro-stack > * + * { margin-top: var(--dpro-space-4); }
.dpro-grid { display: grid; gap: var(--dpro-space-5); grid-template-columns: repeat(auto-fit, minmax(min(100%, 240px), 1fr)); }
.dpro-section { padding-block: var(--dpro-space-7); }
.dpro-muted { color: var(--dpro-warm-gray-600); }

.dpro-button {
  display: inline-flex; align-items: center; justify-content: center; gap: .5rem;
  min-width: 44px; min-height: 44px; padding: .72rem 1rem;
  border: 1px solid transparent; border-radius: var(--dpro-radius-md);
  text-decoration: none; font-weight: 700; line-height: 1.2; cursor: pointer;
  transition: transform .18s ease, box-shadow .18s ease, background-color .18s ease, border-color .18s ease;
}
.dpro-button:hover { transform: translateY(-1px); }
.dpro-button--primary { color: var(--dpro-white); background: var(--dpro-green); box-shadow: var(--dpro-shadow-sm); }
.dpro-button--primary:hover { background: var(--dpro-green-strong); }
.dpro-button--secondary { color: var(--dpro-deep-green); background: var(--dpro-white); border-color: var(--dpro-warm-gray-300); }
.dpro-button--line { color: var(--dpro-white); background: var(--dpro-line-green); }
.dpro-button--demo { color: var(--dpro-deep-green); background: var(--dpro-soft-green); border-color: rgba(22,132,91,.25); }
.dpro-button--flyer { color: var(--dpro-warm-gray-900); background: var(--dpro-warm-gray-100); border-color: var(--dpro-warm-gray-300); }

.dpro-card { min-width: 0; padding: var(--dpro-space-5); border: 1px solid var(--dpro-warm-gray-100); border-radius: var(--dpro-radius-lg); background: var(--dpro-white); box-shadow: var(--dpro-shadow-sm); }
.dpro-card--soft { background: var(--dpro-soft-green); }
.dpro-card__title { margin: 0 0 var(--dpro-space-2); font-size: var(--dpro-font-lg); color: var(--dpro-deep-green); }

.dpro-breadcrumb { display: flex; flex-wrap: wrap; gap: .35rem .55rem; align-items: center; padding: .65rem 0; color: var(--dpro-warm-gray-600); font-size: var(--dpro-font-sm); }
.dpro-breadcrumb a { color: inherit; text-decoration-thickness: 1px; text-underline-offset: .18em; }

.dpro-price { display: flex; flex-wrap: wrap; align-items: baseline; gap: .35rem .7rem; }
.dpro-price__amount { font-size: var(--dpro-font-2xl); line-height: 1; font-weight: 800; color: var(--dpro-deep-green); }
.dpro-price__note { font-size: var(--dpro-font-sm); color: var(--dpro-warm-gray-600); }

.dpro-faq { border-top: 1px solid var(--dpro-warm-gray-300); }
.dpro-faq details { border-bottom: 1px solid var(--dpro-warm-gray-300); }
.dpro-faq summary { min-height: 44px; display: flex; align-items: center; padding: .9rem 0; font-weight: 700; cursor: pointer; }
.dpro-faq details > :not(summary) { margin: 0; padding: 0 0 1rem; }

.dpro-live-preview { overflow: hidden; border: 1px solid var(--dpro-warm-gray-300); border-radius: var(--dpro-radius-lg); background: var(--dpro-warm-gray-50); box-shadow: var(--dpro-shadow-md); }
.dpro-live-preview__placeholder { display: grid; place-items: center; min-height: 260px; padding: var(--dpro-space-5); text-align: center; }
.dpro-live-preview iframe { display: block; width: 100%; min-height: 620px; border: 0; background: var(--dpro-white); }

.dpro-sticky-mobile-cta { display: none; }
.dpro-nav-toggle { min-width: 44px; min-height: 44px; }
[data-dpro-nav][hidden] { display: none !important; }

.dpro-reveal-ready [data-dpro-reveal] { opacity: 0; transform: translateY(12px); transition: opacity .45s ease, transform .45s ease; }
.dpro-reveal-ready [data-dpro-reveal].is-visible { opacity: 1; transform: none; }

[data-dpro-search-item][hidden] { display: none !important; }

@media (max-width: 640px) {
  .dpro-container { width: min(calc(100% - 1.25rem), var(--dpro-content)); }
  .dpro-section { padding-block: var(--dpro-space-6); }
  .dpro-button { width: 100%; }
  .dpro-live-preview iframe { min-height: 560px; }
  .dpro-sticky-mobile-cta {
    position: fixed; z-index: 50; left: 0; right: 0; bottom: 0;
    display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); gap: .5rem;
    padding: .6rem max(.65rem, env(safe-area-inset-right)) calc(.6rem + env(safe-area-inset-bottom)) max(.65rem, env(safe-area-inset-left));
    background: rgba(255,255,255,.96); border-top: 1px solid var(--dpro-warm-gray-300); box-shadow: 0 -8px 24px rgba(15,61,46,.10);
    backdrop-filter: blur(10px);
  }
  .dpro-sticky-mobile-cta .dpro-button { min-width: 0; padding-inline: .55rem; }
}

@media (max-width: 390px) {
  .dpro-grid { grid-template-columns: 1fr; }
  .dpro-card { padding: 1rem; }
  .dpro-live-preview__placeholder { min-height: 220px; padding: 1rem; }
}

@media (max-width: 320px) {
  :root { --dpro-font-2xl: 1.8rem; }
  .dpro-container { width: min(calc(100% - 1rem), var(--dpro-content)); }
  .dpro-button { overflow-wrap: anywhere; }
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { scroll-behavior: auto !important; transition-duration: .01ms !important; animation-duration: .01ms !important; animation-iteration-count: 1 !important; }
  .dpro-reveal-ready [data-dpro-reveal] { opacity: 1; transform: none; }
}


/* DPRO PRODUCT SITE V3.3 — HOME CONNECTION MOTION */
.dpro-home-connection {
  width: min(calc(100% - 2rem), 1120px);
  margin: 0 auto 2rem;
  display: grid;
  place-items: center;
}

.dpro-home-connection__wrap {
  width: min(100%, 420px);
  display: grid;
  gap: .9rem;
  justify-items: center;
}

.dpro-home-connection__visual {
  position: relative;
  width: min(86vw, 320px);
  aspect-ratio: 1;
  display: grid;
  place-items: center;
}

.dpro-home-connection__orbit,
.dpro-home-connection__orbit::before {
  position: absolute;
  inset: 0;
  border-radius: 50%;
}

.dpro-home-connection__orbit--outer {
  inset: 10px;
  border: 1.5px dashed rgba(22,132,91,.28);
  animation: dproOrbitSpin 24s linear infinite;
}

.dpro-home-connection__orbit--inner {
  inset: 44px;
  border: 1px solid rgba(22,132,91,.16);
  box-shadow: inset 0 0 0 10px rgba(237,247,242,.55);
}

.dpro-home-connection__glow {
  position: absolute;
  inset: 18%;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(22,132,91,.16) 0%, rgba(22,132,91,.06) 46%, rgba(22,132,91,0) 72%);
  filter: blur(6px);
  animation: dproPulseGlow 3.4s ease-in-out infinite;
}

.dpro-home-connection__center {
  position: relative;
  z-index: 3;
  width: 156px;
  min-height: 156px;
  padding: 1rem;
  border-radius: 1.35rem;
  background: linear-gradient(145deg, #0f3d2e, #175741);
  color: #fff;
  box-shadow: 0 18px 46px rgba(15,61,46,.22);
  display: grid;
  place-items: center;
  text-align: center;
  animation: dproHubFloat 4.4s ease-in-out infinite;
}

.dpro-home-connection__center small {
  display: block;
  margin-bottom: .35rem;
  font-size: .68rem;
  font-weight: 900;
  letter-spacing: .16em;
  opacity: .78;
}

.dpro-home-connection__center strong {
  display: block;
  font-size: 1.25rem;
  line-height: 1.15;
  letter-spacing: -.02em;
}

.dpro-home-connection__spinner {
  position: absolute;
  inset: 0;
  animation: dproOrbitSpin 18s linear infinite;
}

.dpro-home-connection__spinner--reverse {
  animation-direction: reverse;
  animation-duration: 14s;
}

.dpro-home-connection__node {
  position: absolute;
  left: 50%;
  top: 6px;
  transform: translateX(-50%);
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 84px;
  min-height: 40px;
  padding: .45rem .85rem;
  border-radius: 999px;
  background: rgba(255,255,255,.96);
  border: 1px solid rgba(22,132,91,.16);
  color: var(--dpro-deep-green);
  box-shadow: var(--dpro-shadow-sm);
  font-size: .78rem;
  font-weight: 900;
  letter-spacing: .04em;
  animation: dproOrbitCounter 18s linear infinite;
}

.dpro-home-connection__spinner--reverse .dpro-home-connection__node {
  top: auto;
  left: auto;
  right: 10px;
  bottom: 50%;
  transform: translateY(50%);
  animation-duration: 14s;
  animation-direction: normal;
}

.dpro-home-connection__node::before {
  content: "";
  width: 10px;
  height: 10px;
  border-radius: 50%;
  margin-right: .45rem;
  background: var(--dpro-green);
  box-shadow: 0 0 0 5px rgba(22,132,91,.12);
}

.dpro-home-connection__node--line::before { background: var(--dpro-line-green); box-shadow: 0 0 0 5px rgba(6,199,85,.12); }
.dpro-home-connection__node--web::before { background: #2a7de1; box-shadow: 0 0 0 5px rgba(42,125,225,.12); }

.dpro-home-connection__trail {
  position: absolute;
  width: 14px;
  height: 14px;
  border-radius: 50%;
  background: rgba(22,132,91,.55);
  top: 50%;
  left: 50%;
  margin: -7px 0 0 -7px;
  box-shadow: 0 0 0 8px rgba(22,132,91,.08);
}

.dpro-home-connection__trail--one {
  animation: dproTrailOrbit 8s linear infinite;
}

.dpro-home-connection__trail--two {
  background: rgba(6,199,85,.55);
  box-shadow: 0 0 0 8px rgba(6,199,85,.08);
  animation: dproTrailOrbitInner 5.6s linear infinite reverse;
}

.dpro-home-connection__caption {
  margin: 0;
  text-align: center;
  color: var(--dpro-warm-gray-600);
  font-size: .94rem;
}

.core-home .core-path__arrow {
  transition: transform .22s ease, color .22s ease;
  animation: dproArrowPulse 2s ease-in-out infinite;
}

.core-home .core-path .core-card--accent {
  animation: dproCardGlow 4s ease-in-out infinite;
}

@keyframes dproOrbitSpin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

@keyframes dproOrbitCounter {
  from { transform: translateX(-50%) rotate(0deg); }
  to { transform: translateX(-50%) rotate(-360deg); }
}

@keyframes dproTrailOrbit {
  from { transform: rotate(0deg) translateX(146px) rotate(0deg); }
  to { transform: rotate(360deg) translateX(146px) rotate(-360deg); }
}

@keyframes dproTrailOrbitInner {
  from { transform: rotate(0deg) translateX(98px) rotate(0deg); }
  to { transform: rotate(360deg) translateX(98px) rotate(-360deg); }
}

@keyframes dproPulseGlow {
  0%, 100% { transform: scale(.94); opacity: .7; }
  50% { transform: scale(1.03); opacity: 1; }
}

@keyframes dproHubFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-6px); }
}

@keyframes dproArrowPulse {
  0%, 100% { transform: translateX(0); opacity: .75; }
  50% { transform: translateX(4px); opacity: 1; }
}

@keyframes dproCardGlow {
  0%, 100% { box-shadow: 0 10px 24px rgba(15,61,46,.12); }
  50% { box-shadow: 0 18px 34px rgba(15,61,46,.2); }
}

@media (max-width: 900px) {
  .dpro-home-connection__wrap { width: min(100%, 360px); }
  .dpro-home-connection__visual { width: min(84vw, 280px); }
  .dpro-home-connection__center { width: 144px; min-height: 144px; }
}

@media (max-width: 700px) {
  .dpro-home-connection { margin-bottom: 1.25rem; }
  .dpro-home-connection__visual { width: min(82vw, 244px); }
  .dpro-home-connection__orbit--inner { inset: 32px; }
  .dpro-home-connection__center { width: 132px; min-height: 132px; border-radius: 1.1rem; }
  .dpro-home-connection__center strong { font-size: 1.05rem; }
  .dpro-home-connection__node { min-width: 74px; min-height: 36px; padding: .38rem .72rem; font-size: .72rem; }
  .dpro-home-connection__spinner--reverse .dpro-home-connection__node { right: 4px; }
  @keyframes dproTrailOrbit {
    from { transform: rotate(0deg) translateX(112px) rotate(0deg); }
    to { transform: rotate(360deg) translateX(112px) rotate(-360deg); }
  }
  @keyframes dproTrailOrbitInner {
    from { transform: rotate(0deg) translateX(78px) rotate(0deg); }
    to { transform: rotate(360deg) translateX(78px) rotate(-360deg); }
  }
}

@media (prefers-reduced-motion: reduce) {
  .dpro-home-connection__orbit--outer,
  .dpro-home-connection__spinner,
  .dpro-home-connection__node,
  .dpro-home-connection__trail,
  .dpro-home-connection__glow,
  .dpro-home-connection__center,
  .core-home .core-path__arrow,
  .core-home .core-path .core-card--accent {
    animation: none !important;
  }
}
